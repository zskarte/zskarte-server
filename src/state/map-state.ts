const defaultState = {};
const mapState = {};

const operations = [];

const initMapStates = () => {
  console.log();
};

const addMapState = (operation) => {};

export { defaultState, mapState, initMapStates, addMapState };
