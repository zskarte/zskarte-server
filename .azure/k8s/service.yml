apiVersion: v1
kind: Service
metadata:
  namespace: ${KUBERNETES_NAMESPACE}
  name: ${APP_NAME}
spec:
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: ${APP_NAME}
    service: api
    stage: ${STAGE}
